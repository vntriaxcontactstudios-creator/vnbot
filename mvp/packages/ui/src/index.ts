/**
 * VNBOT UI — Shared component library index.
 *
 * Re-exports from @vnbot/pixelart + provides shell components.
 */

export { MascotStateView, PixelPanel, PixelBackground, PixelAvatar, PixelTile } from '@vnbot/pixelart';
export type { AgentId, MascotState, SpriteSize } from '@vnbot/pixelart';
